# Copy to .env (do not commit .env)
#
# APNs credentials (required)
APNS_TEAM_ID=YOUR_TEAM_ID
APNS_KEY_ID=YOUR_KEY_ID
APNS_BUNDLE_ID=com.example.app

# Prefer a .p8 file path (recommended)
APNS_P8_PATH=E:\\path\\to\\AuthKey_XXXXXXXXXX.p8

# Alternatively, inline PEM (not recommended on Windows due to multiline escaping)
# APNS_P8_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Default environment
APNS_ENV=production

# Optional default token (can also pass via CLI flag)
APNS_DEVICE_TOKEN=YOUR_DEVICE_TOKEN_64_HEX

# Optional proxy (China mainland typical)
# HTTP_PROXY=http://127.0.0.1:7897
# HTTPS_PROXY=http://127.0.0.1:7897
